# Dépendances Python du pipeline MVS Audiobook
# Installation : pip install -r requirements.txt

# Pipeline async
aiohttp>=3.9.0
aiofiles>=23.2.0

# Dashboard
fastapi>=0.109.0
uvicorn[standard]>=0.27.0

# Audio (si FFmpeg pas dispo en PATH, installe via apt)
# sudo apt install ffmpeg
